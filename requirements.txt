# Brand Reputation Analyzer - Requirements
# Versione Multi-Platform con SERP Integration

# Core Streamlit Framework
streamlit>=1.28.0

# Data Processing & Analysis
pandas>=1.5.0
numpy>=1.21.0

# AI/ML Libraries
openai>=1.3.0
scikit-learn>=1.3.0

# Web Scraping & HTTP
requests>=2.28.0
beautifulsoup4>=4.11.0
lxml>=4.9.0
html5lib>=1.1

# Text Processing & NLP
difflib  # Built-in Python module

# Excel Export
openpyxl>=3.0.10
xlsxwriter>=3.0.3

# Async & Concurrency
concurrent.futures  # Built-in Python module

# URL Parsing
urllib3>=1.26.0

# Regular Expressions
re  # Built-in Python module

# JSON Processing
json  # Built-in Python module

# Time Management
time  # Built-in Python module

# Warnings Management
warnings  # Built-in Python module

# I/O Operations
io  # Built-in Python module

# Collections
collections  # Built-in Python module

# Random Operations
random  # Built-in Python module

# Additional utility libraries for web scraping
selenium>=4.15.0  # Optional: for JavaScript-heavy sites
webdriver-manager>=4.0.0  # Optional: for Selenium WebDriver management

# HTTP Headers and User Agents
fake-useragent>=1.4.0  # Optional: for rotating user agents

# Rate Limiting
ratelimit>=2.2.1  # Optional: for better rate limiting control

# Progress Bars (if needed outside Streamlit)
tqdm>=4.65.0  # Optional: for console progress bars

# Date/Time Processing
python-dateutil>=2.8.0  # For better date parsing

# Text Similarity (for deduplication)
fuzzywuzzy>=0.18.0  # Optional: for fuzzy string matching
python-Levenshtein>=0.20.0  # Optional: for faster fuzzy matching

# Logging (for debugging)
logging  # Built-in Python module

# Environment Variables (for API keys)
python-dotenv>=1.0.0  # Optional: for .env file support

# HTTP Session Management
requests-cache>=1.1.0  # Optional: for caching HTTP requests

# Data Validation
pydantic>=2.0.0  # Optional: for data validation

# Configuration Management
pyyaml>=6.0  # Optional: for YAML config files

# Threading (already built-in but listing for clarity)
threading  # Built-in Python module

# Type Hints
typing  # Built-in Python module (Python 3.5+)
typing-extensions>=4.7.0  # For advanced type hints
